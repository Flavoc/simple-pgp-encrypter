### when right control key is pressed, program will select whole text (ctrl-A) and encrypt it. 
you must edit file and change pgp public key and certificate id to yours.
if program is not working be sure to install required packages for this script using pip in cmd `pip install -r requirements.txt`.
because of i am beginner, the script may contain bugs. i implemented panic button which closes program. panic button can be used by clicking insert on keyboard
